# Адрес бэкенда
VITE_API_URL=http://localhost:8090

# Код подтверждения домена для TikTok (URL properties → verify by file).
# Из него на сборке генерируется /tiktok<код>.txt в корне сайта.
# Значение берётся из имени скачанного файла: tiktok<КОД>.txt
TIKTOK_SITE_VERIFICATION=
